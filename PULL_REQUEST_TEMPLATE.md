# Pull request

**Summary** (what and why)

**Checklist**
- [ ] `python -m pytest tests -q` passes locally
- [ ] `python scripts/check_geometry_conventions.py` passes
- [ ] No data files added that fall under the link-only / forbidden rulings
      (see `THIRD_PARTY_NOTICES.md` and `.gitignore` policy blocks)
- [ ] New numbers quoted in docs carry a pointer to their source JSON
